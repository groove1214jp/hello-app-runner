version: 1.0
runtime: python3
build:
  commands:
    build:
    - yum install -y pycairo
    - pip install -r requirements.txt
run:
  command: python app.py
  env: MY_STRING
  network:
    port: 8000
env:
  - name: MY_STRING
    value: "TEST-ENV"
